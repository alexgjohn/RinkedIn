{% extends 'base.jinja' %}

{% block content %}
<h1>{{ title }}</h1>

{% for lesson in lessons %}

    <p class="lesson_name">
        <a href="/lessons/{{ lesson.id }}">{{ lesson.name }}</a>, every {{ lesson.day}}!
    </p>

{% endfor %}

{% endblock %}